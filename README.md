# Hand Tracking Cursor

A hand tracking cursor you can drop onto any website with one `<script>` tag.

Powered by [MediaPipe Hand Landmarker](https://ai.google.dev/edge/mediapipe/solutions/vision/hand_landmarker).
Everything runs on-device — the camera stream never leaves the browser.

